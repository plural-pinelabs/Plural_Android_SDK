<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/white"
    android:id="@+id/constrain_upi_intent"
    android:visibility="gone">

    <Button
        android:id="@+id/btn_proceed_pay"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp"
        android:background="@drawable/button_curved"
        android:backgroundTint="@null"
        android:fontFamily="sans-serif-medium"
        android:text="@string/pay_by_upi"
        android:textColor="@color/black"
        android:textSize="15sp"
        app:backgroundTint="@null"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_upi_apps"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:background="@drawable/rounded_corner"
        app:layout_constraintLeft_toLeftOf="@+id/btn_proceed_pay"
        app:layout_constraintRight_toRightOf="@id/btn_proceed_pay"
        app:layout_constraintTop_toBottomOf="@id/btn_proceed_pay" />
</androidx.constraintlayout.widget.ConstraintLayout>