<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/rounded_corner"
    android:backgroundTint="@color/shimmer_grey"
    android:padding="20dp">

    <ImageView
        android:id="@+id/img_unlock"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/saved_card_unlock"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/txt_unlock_saved_card"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="20dp"
        android:fontFamily="sans-serif-medium"
        android:text="@string/unlock_saved_card"
        android:textColor="@color/black"
        android:textSize="16sp"
        android:textFontWeight="500"
        app:layout_constraintLeft_toRightOf="@+id/img_unlock"
        app:layout_constraintTop_toTopOf="@+id/img_unlock" />

    <TextView
        android:id="@+id/txt_unlock_saved_card_info"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:alpha=".8"
        android:textFontWeight="500"
        android:fontFamily="sans-serif-medium"
        android:text="@string/verify_saved_card"
        android:textColor="@color/black"
        android:textSize="14sp"
        app:layout_constraintLeft_toLeftOf="@+id/txt_unlock_saved_card"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/txt_unlock_saved_card" />
</androidx.constraintlayout.widget.ConstraintLayout>